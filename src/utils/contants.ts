export enum PostType {
  Personal = "personal",
  Study = "study",
  School = "school",
}

export enum PostPrimary {
    PUBLIC = "public",
    FRIEND = "friend",
  }

  export enum UserStatus {
    Connect = "Connect",
    Requested = "Requested",
    Connected = "Connected",
  }
